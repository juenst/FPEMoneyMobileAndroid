package lib.finpay.sdk.uikit.utilities

enum class SharedPrefKeys {
    SHARED_NAME,
    TOKEN,
    LANGUAGE,
    USER_ID,
    IS_LOGIN,
    FIREBASE_TOKEN,
    LATITUDE,
    LONGITUDE,
    TYPE_USER,
}